TEMPLATE FILE — replace with your project-specific data
# LRU project management

**Purpose:** optional active project slots plus archive and `project-list.md`.

If you multi-repo, track other repos in your global memory system or add `.sentinel-ai/projects/` with `active/`, `archived/`, and `project-list.md`.

No LRU engine is vendored here by default.
