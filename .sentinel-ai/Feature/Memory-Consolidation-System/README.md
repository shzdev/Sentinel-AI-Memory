TEMPLATE FILE — replace with your project-specific data
# Memory consolidation

**Purpose:** merge fragmented notes into a unified format with clear canonical ownership.

## Baseline

- Canonical content lives under `main/`.
- Router is `master-memory.md`.
- Decision logic lives under `decision-engine/`.
- Governance rules live under `governance/`.
- Memory write discipline lives under `memory-policy/`.
- Root alias files point back to `main/` only.

No separate patch installer is required for this layout.
