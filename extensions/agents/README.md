# Optional Agents

These are lightweight prompts for focused review tasks.

- They are optional helpers, not core logic
- They must follow Sentinel governance
- They should ask for missing context instead of guessing
- They should avoid broad or destructive changes
