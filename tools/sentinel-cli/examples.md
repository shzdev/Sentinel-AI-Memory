# Examples

Design-only examples for the future `sentinel install skill` command.

```bash
sentinel install skill readme-improver
sentinel install skill architecture-auditor --dry-run
sentinel import skill ./openai-style-skill --type openai-compatible --dry-run
sentinel install skill ./local-skill --source local
```

These examples are references only. They are not executable in this repository yet.
