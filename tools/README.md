# Tools

This directory holds design-only tooling concepts for future Sentinel AI extension operations.

Rules:

- these files are documentation scaffolds
- they do not make the CLI executable
- they do not change Sentinel core behavior
- they stay optional and registry-driven

Current focus:

- `sentinel-cli/` for future extension installation concepts
